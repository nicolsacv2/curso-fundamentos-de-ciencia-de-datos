# pnpm writes `packageManager` into package.json when it runs, pinning the exact
# version it happens to be. That pin broke the host build once already: corepack then
# refuses whatever version the builder has. Off, so the field never comes back.
manage-package-manager-versions=false
